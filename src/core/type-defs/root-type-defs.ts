import root from './root-schema';
import rootInputs from './root-inputs';
import rootScalars from './root-scalars';

const rootTypeDefs = [root, rootInputs, rootScalars];

export default rootTypeDefs;